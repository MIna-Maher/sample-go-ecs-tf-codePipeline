[
    {
      "name": "${name}",
      "image": "${repo_url}:latest",
      "memory": ${app_memory},
      "cpu": ${app_cpu},
      "essential": true,
      "portMappings": [
        {
          "hostPort": ${app_port},
          "containerPort": ${app_port},
          "protocol": "tcp"
        }
      ]
    }
  
]